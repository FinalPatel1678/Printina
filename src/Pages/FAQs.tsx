import { createStyles, makeStyles } from '@material-ui/core/styles';
import React from 'react';

const useStyles = makeStyles(() =>
  createStyles({

  }),
);

const FAQs: React.FC = () => {
  const classes = useStyles();

  return (
    <React.Fragment>FAQs.tsx</React.Fragment>
  );
}
export default FAQs;